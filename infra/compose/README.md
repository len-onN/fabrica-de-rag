# Compose

Reservado para os arquivos Docker Compose.

Arquivos planejados:

- `compose.yml`;
- `compose.dev.yml`;
- `compose.e2e.yml`.

As imagens devem ser versionadas, sem `latest`. O compose real entra em `build/dev-runtime-compose`.

