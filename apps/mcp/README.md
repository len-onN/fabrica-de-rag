# MCP

Reservado para o servidor MCP.

Escopo futuro:

- TypeScript em Node 24;
- tools guiadas e versionadas;
- schemas explicitos;
- chamadas ao backend, sem acesso direto a Postgres, Qdrant ou storage;
- identidade, workspace scope, limites e analytics local via contratos aprovados.

A implementacao de tools entra em `feat/mcp-tools-base`.

