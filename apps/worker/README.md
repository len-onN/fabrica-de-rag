# Worker

Reservado para o worker Python.

Escopo futuro:

- Python 3.13 com layout `src`;
- FastAPI para API interna;
- contratos Pydantic versionados;
- processamento de PDF, OCR, tabelas, imagens e embeddings;
- testes com fixtures pequenas.

A branch `chore/worker-python-base` cria o scaffold executavel.

