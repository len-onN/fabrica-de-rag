-- Baseline migration
-- O schema documental e tabelas especificas serao criadas em proximas branches.
-- Este script garante a criacao inicial do Flyway schema history e extensões se necessarias.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
